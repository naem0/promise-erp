import StudentEarningsFilter from "./StudentEarningsFilter"

export default async function StudentEarningsFilterData() {
    // No extra data needed for student earnings filter based on current API
    // But keeping the Data wrapper pattern for consistency with other modules

    return (
        <StudentEarningsFilter />
    );
}
