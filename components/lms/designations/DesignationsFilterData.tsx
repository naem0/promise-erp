import DesignationsFilter from "./DesignationsFilter";

export default async function DesignationsFilterData() {
    // Currently no external data needed for designations filter
    // But following the structure of EmployeesFilterData
    
    return (
        <DesignationsFilter />
    );
}
