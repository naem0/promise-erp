import BrandsFilter from "./BrandsFilter";

export default async function BrandsFilterData() {
    // No extra data needed for brands filter based on current API
    // But keeping the Data wrapper pattern for consistency with other modules
    
    return (
        <BrandsFilter />
    );
}
